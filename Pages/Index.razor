@page "/"


<TabRoot>
    <TabChild Text="Item 1">
        <TabRoot>
            <TabChild Text="Item 1.1">
            </TabChild>
            <TabChild Text="Item 1.2">
            </TabChild>
            <TabChild Text="Item 1.3">
                <TabRoot>
                    <TabChild OnClickCallback="@ShowMessage" Text="Item 1.3.1">
                    </TabChild>
                    <TabChild Text="Item 1.3.2">
                    </TabChild>
                    <TabChild Text="Item 1.3.3">
                    </TabChild>
                </TabRoot>
            </TabChild>
        </TabRoot>
    </TabChild>
    <TabChild Text="Tab 2">
    </TabChild>
    <TabChild Text="Tab 3">
    </TabChild>
</TabRoot>
<p>@message</p>

@code {
    private string message;

    //TO DO Implement recursive component to render the generated elements.
    //Additionally the classes holding the structure can be enhanced to store the required attributes from the components
    //private List<RootElement> generatedElements = GenereteNestedMenu.GenerateMenu();

    //TO DO Style the nested sub menu to have additional margin-left from the previos parent


    private void ShowMessage(MouseEventArgs e)
    {
        message = $"Event Handled!";
    }
}